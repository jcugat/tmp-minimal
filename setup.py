from setuptools import setup

setup(
    name='minimal',
    version='0.1',
    description='minimal',
    url='https://www.python.org',
    author='Me',
    packages=['minimal'],
    zip_safe=False,
)
